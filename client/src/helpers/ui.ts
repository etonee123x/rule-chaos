export const UI = Object.freeze({
  CONTAINER: 'container',
  BUTTON: Object.freeze({
    _name: 'button',
  }),
  ICON: Object.freeze({
    _name: 'icon',
  }),
  ELEMENT_TITLE: 'element-title',
  CHECKBOX: Object.freeze({
    default: 'checkbox',
    SIZE: Object.freeze({
      SM: 'checkbox_size_sm',
      XS: 'checkbox_size_xs',
    }),
  }),
});
